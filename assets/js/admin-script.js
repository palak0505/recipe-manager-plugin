jQuery(document).ready(function($) {
    // Your admin JavaScript can go here.
    // Example: Show a confirmation for a specific field
    // $('#rmp_difficulty').on('change', function() {
    //     if ($(this).val() === 'hard') {
    //         alert('Setting difficulty to Hard! Make sure the instructions are very clear.');
    //     }
    // });
});